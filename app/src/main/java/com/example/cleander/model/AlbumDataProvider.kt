package com.example.cleander.model

import com.example.cleander.R

object AlbumDataProvider {
    val photos = mutableListOf(Album(
        id = 1,
        date = "ciao",
        imageId = R.drawable.undraw_done_checking_splash,
        swiped = false
    )
    )
}